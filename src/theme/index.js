// GiftBox4you Theme - Main Export

import { colors } from './colors';
import { typography, textStyles } from './typography';
import { spacing, borderRadius, shadows, iconSizes, avatarSizes } from './spacing';

export const theme = {
  colors,
  typography,
  textStyles,
  spacing,
  borderRadius,
  shadows,
  iconSizes,
  avatarSizes,
};

export {
  colors,
  typography,
  textStyles,
  spacing,
  borderRadius,
  shadows,
  iconSizes,
  avatarSizes,
};

export default theme;
